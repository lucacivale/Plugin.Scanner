<Page
    Background="{ThemeResource BackgroundBrush}"
    NavigationCacheMode="Required"
    x:Class="Plugin.Scanner.Uno.Tests.Presentation.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:utu="using:Uno.Toolkit.UI"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer IsTabStop="True">
        <Grid utu:SafeArea.Insets="VisibleBounds">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition />
            </Grid.RowDefinitions>
            <utu:NavigationBar Content="Plugin.Scanner.Uno" />

            <StackPanel
                Grid.Row="1"
                HorizontalAlignment="Center"
                Spacing="16"
                VerticalAlignment="Center">
                <Button Command="{Binding ScanBarcode}" Content="San barcode" />
                <TextBlock Text="{Binding RecognizedItem, Mode=TwoWay}" />
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>
